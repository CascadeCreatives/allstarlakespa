/**
 * Visibility switches for individual service offerings.
 *
 * Set a flag back to `true` to bring that service back everywhere at once:
 * its block on /services, its card on the homepage, its footer link, and its
 * entry in the LocalBusiness structured data.
 *
 * Hidden September 2026 while the spa operates from the temporary
 * Bellewood Place location.
 */

export const showBodyTreatments = false;
export const showSpaPackages = false;
